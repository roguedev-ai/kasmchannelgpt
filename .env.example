# ============================================
# RAG Platform Environment Configuration
# ============================================

# Backend Configuration
JWT_SECRET=your-jwt-secret-min-32-chars
DATABASE_PATH=./data/rag-platform.db
QDRANT_URL=http://localhost:6333

# ============================================
# Embedding Provider Configuration
# Choose: openai or gemini
# ============================================
EMBEDDING_PROVIDER=openai

# OpenAI Configuration (if EMBEDDING_PROVIDER=openai)
# Can use CustomGPT key as fallback for embeddings
OPENAI_API_KEY=sk-your-openai-key
# OR
CUSTOMGPT_API_KEY=sk-your-customgpt-key

# Google Gemini Configuration (if EMBEDDING_PROVIDER=gemini)
GEMINI_API_KEY=your-gemini-api-key

# ============================================
# CustomGPT Configuration
# ============================================

# Base Configuration
CUSTOMGPT_API_KEY=your-customgpt-api-key
CUSTOMGPT_BASE_URL=https://app.customgpt.ai/api/v1

# Default Agent (fallback for all functions)
CUSTOMGPT_DEFAULT_AGENT_ID=123456

# Function-Specific Agents
# If not set, falls back to CUSTOMGPT_DEFAULT_AGENT_ID
CUSTOMGPT_AGENT_SALES=111111      # Sales inquiries, pricing
CUSTOMGPT_AGENT_SUPPORT=222222    # Customer support, troubleshooting
CUSTOMGPT_AGENT_TECHNICAL=333333  # Technical docs, API questions
CUSTOMGPT_AGENT_GENERAL=444444    # General information

# ============================================
# File Processing Configuration
# ============================================
MAX_FILE_SIZE=10485760  # 10MB in bytes
ALLOWED_FILE_TYPES=text/plain,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document
CHUNK_SIZE=1000
CHUNK_OVERLAP=200

# ============================================
# Security Configuration
# ============================================
JWT_EXPIRATION_HOURS=24

# ============================================
# Important Notes
# ============================================

# 1. Agent Functions
# - Each function (sales, support, technical, general) can have a dedicated agent
# - If a function's agent is not set, it falls back to DEFAULT_AGENT_ID
# - If no agents are configured, all queries use DEFAULT_AGENT_ID

# 2. Function Detection
# - System auto-detects query intent based on keywords
# - Sales: pricing, purchase, billing, etc.
# - Support: help, issues, problems, etc.
# - Technical: api, integration, setup, etc.
# - General: fallback for unmatched queries

# 3. API Keys
# - When using OpenAI embeddings, you can use either OPENAI_API_KEY
#   or CUSTOMGPT_API_KEY (will use the same key for both)
# - When using Gemini embeddings, GEMINI_API_KEY is required
# - CUSTOMGPT_API_KEY is always required for LLM responses

# 4. Security
# - JWT_SECRET must be at least 32 characters
# - Use different API keys for development and production
# - Rotate keys regularly

# 5. File Processing
# - MAX_FILE_SIZE is in bytes (default: 10MB)
# - CHUNK_SIZE affects token usage and context window
# - Larger CHUNK_OVERLAP may improve relevance but increases storage

# ============================================
# Development Configuration
# ============================================
NODE_ENV=development
DEBUG=false
